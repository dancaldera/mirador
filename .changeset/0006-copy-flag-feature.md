---
"seerdb": minor
---

Added --copy flag to main CLI for copying agent documentation to clipboard. Cross-platform support for macOS (pbcopy), Linux (xclip), and Windows (clip). Removed separate scripts folder as functionality is now integrated into main CLI.