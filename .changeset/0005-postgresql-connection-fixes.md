---
"seerdb": patch
---

Fixed PostgreSQL connection issues for trust authentication and localhost connections. Added support for individual connection parameters in headless mode, improved SSL configuration for local connections, and fixed password handling for trust authentication.